[NEAT]
fitness_criterion     = max
fitness_threshold     = 1.0
pop_size              = 150
reset_on_extinction   = False

[HyperNEAT]
fitness_criterion    = max
fitness_threshold    = 3.5
pop_size             = 150
reset_on_extinction  = False

[CPPN]
# CPPN-specific settings for generating the ANN weights
num_inputs           = 4  # Includes x, y for both source and target
num_outputs          = 1  # Weight of the connection
num_hidden           = 2  # Example value, adjust based on complexity
initial_connection   = full_nodirect  # Connect every input to every output

activation_default   = sigmoid
activation_options   = sigmoid, sin, gaussian, relu

# Network topology for CPPN
z_axis_weight        = 0.1  # Example: influence of the z-axis on connection weight
max_weight           = 5.0
min_weight           = -5.0

# CPPN mutation rates
weight_mutate_rate   = 0.8
connection_mutate_rate = 0.05
node_mutate_rate     = 0.03

[Substrate]
# Define the layout of neurons in the substrate
input_coordinates    = [(-1.0, -1.0, 0.0), (1.0, -1.0, 0.0)]  # Example for 2D input
output_coordinates   = [(0.0, 1.0, 0.0)]  # Example for 1D output

hidden_layers        = 1  # Number of hidden layers in the substrate
neurons_per_layer    = 10  # Neurons in each hidden layer

[DefaultGenome]
# Similar to NEAT but adapted for CPPN
num_inputs           = 4
num_outputs          = 1
num_hidden           = 2
initial_connection   = full_nodirect

activation_default   = sigmoid
activation_options   = sigmoid, sin, gaussian, relu

feed_forward         = True
conn_add_prob        = 0.5
conn_delete_prob     = 0.5

[DefaultSpeciesSet]
compatibility_threshold = 3.0

[DefaultStagnation]
species_fitness_func = max
max_stagnation       = 20
species_elitism      = 2

[DefaultReproduction]
elitism              = 2
survival_threshold   = 0.2
